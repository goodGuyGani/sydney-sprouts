import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.deliverysystem.mobile',
  appName: 'Delivery System',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config

